# 上号吧 · 正式版开发交接文档

> 本文档用于在新的会话中指导「上号吧」微信小程序的**正式版本**开发。
> 阅读完本文后，新会话无需再翻查 demo 代码即可全面理解项目，并可直接开始正式开发。
>
> **重要约定（务必先读）**：本文中凡是标注 🔒 **【必须遵循】** 的内容，是正式版的硬性需求与已验证的业务约束，**不可偏离**；凡是标注 🔍 **【demo 仅供参考】** 的内容，仅说明「当前 demo 是怎么实现/验证的」，**前端界面与实现方式可在正式版中全部重做**，但背后的业务规则与数据契约仍需遵守。

---

## 1. 项目背景与目标

### 1.1 核心用途
「上号吧」是一款面向**熟人圈子**的微信小程序，解决一个很具体的痛点：**一群朋友想一起打某一款游戏时，缺乏一个轻量、即时、无需加群的「组队召集」工具**。用户发起一个上号房间，选好游戏、模式、人数、开打时间，把房间分享给朋友，朋友加入后准备、房主确认开始，完成一次「喊人开黑」的闭环。

### 1.2 要解决的问题
- **召集效率低**：微信群里 @ 人、接龙、问「几点」「几排」非常碎片化。
- **状态不可见**：谁来了、谁准备好了、满没满，需要反复口头确认。
- **无轻量工具**：不希望为了开黑专门建群或下载额外 App。

### 1.3 目标用户与场景
- 目标用户：有固定/半固定游戏搭子的普通玩家。
- 典型场景：午休/下班前想约一波《无畏契约》《三角洲行动》《CS2》《英雄联盟》《永劫无间》等，快速拉人、看准备状态、到点开打。

### 1.4 商业与隐私边界 🔒
- **不收集任何游戏账号、密码**。仅保存用户自填的「游戏昵称」用于房间内展示。
- 游戏昵称属隐私，存储在 `users` 集合且权限设为「仅创建者可读写」（见 §4.2）。

---

## 2. 当前 demo 现状

> 说明：当前仓库里同时存在两份前端实现——
> 1. `miniprogram/`：真实的微信小程序前端（WXML/WXSS/JS），可在微信开发者工具中运行。
> 2. `demo/index.html`：一个在**浏览器**中运行的 HTML 镜像，用 `localStorage` 模拟数据库，仅用于 **Playwright 端到端测试**，并非真实应用。
>
> 两者都**已实现并验证**了下面的核心功能与业务流程；但正式版的前端**将全部重新设计**（见 §3），此处仅记录「已经跑通的业务逻辑」供新会话复用。

### 2.1 已验证可行的核心功能
- ✅ 发起上号（选游戏 / 人数 / 模式备注 / 开打时间）
- ✅ 房间大厅列表（只显示招募中且未过期的房间 + 我的房间）
- ✅ 加入房间（含通过分享链接自动入房）
- ✅ 房间内成员管理（房主可见移除按钮；准备状态展示）
- ✅ 准备 / 取消准备（全员准备后进入「待房主开始」）
- ✅ 房主开始游戏（锁房 + 全员就绪态）
- ✅ 退出 / 移除队员 / 解散房间
- ✅ 房间内文字聊天（实时）
- ✅ 设置游戏昵称
- ✅ 房间 2 小时无活动自动过期（列表隐藏）

### 2.2 已验证的业务逻辑（重做前端时务必保留）🔒
- **人数约束**：每房 2–20 人；各游戏默认人数（无畏契约 5 / 三角洲行动 4 / CS2 5 / 英雄联盟 5 / 永劫无间 3）。
- **满员规则**：成员数达到 `maxPlayers` 即视为满员，新成员被拒（提示「房间已满，下次早点来」）。
- **准备状态机**：成员数 ≥ 2 且**全部准备** → 房间进入 `pending`（待房主开始）；任一成员取消准备/退出 → 回落为 `recruiting`，但其余成员的准备状态**保留**。
- **锁房时机**：仅当房主在 `pending` 点击「开始游戏」后才真正进入 `ready`（锁房，拒绝新成员加入）。
- **软删除**：解散/移除只是把房间标记为 `dissolved`，文档不物理删除；前端靠 `watch` 收到状态变化后自动跳走。
- **过期机制**：房间创建时写入 `expireAt = createdAt + 2 小时`；列表查询只返回 `expireAt > now` 的房间，过期房间从大厅消失（但数据保留，无定时清理任务）。
- **并发安全**：`joinRoom` 在并发抢名额时做了「二次 count 校验 + 补偿删除」，避免超员；`toggleReady` 使用数据库事务保证状态一致。

### 2.3 已验证的关键流程 🔍
- **分享进入**：`room-detail` 页面 `onLoad` 时若通过分享链接进入且当前用户非成员，会先自动调用 `joinRoom` 再加入，保证「分享即入房」。
- **实时刷新**：房间详情页同时 `watch` 三个集合（房间、成员、消息），任一变化触发重新拉取全量数据并 `setData`。
- **全员准备提醒（订阅消息）**：代码链路已接通（`startRoom` 调 `cloud.openapi.subscribeMessage.send`），但**模板未配置**，当前行为为「开始成功但提示『开打提醒尚未配置』」，不会伪造已推送结果。

---

## 3. 保留与重做清单

### 3.1 🔒 必须在正式版保留的需求（需求层面，不可变）
| 类别 | 保留项 |
|---|---|
| 业务规则 | §2.2 全部约束：人数 2–20、各游戏默认人数、满员拒入、准备状态机、锁房时机、软删除、2h 过期、并发安全 |
| 数据契约 | §4.1 的四个集合字段定义、§4.3 的 12 个云函数入参与返回结构 |
| 权限模型 | `rooms/participants/messages` 集合权限「所有用户可读，仅创建者可读写」；`users`「仅创建者可读写」 |
| 接口行为 | 各云函数的校验逻辑、错误提示文案语义、边界处理（如房主不能退出、非成员不能发消息等） |
| 实时架构 | 继续使用云数据库 `watch` 做客户端实时监听（不引入 WebSocket 等额外基建） |
| 订阅消息流程 | 保留 `pending → 房主开始 → ready + 发订阅消息` 的触发链与「未配置则明确提示」的诚实行为 |

### 3.2 🔄 需要彻底重做的部分（重点：前端设计）
- **🎯 前端界面与交互（全部重做）**：`miniprogram/` 下的所有 **WXML 布局、WXSS 样式、页面视觉/交互细节** 将在正式版中重新设计。**`demo/index.html` 的界面实现仅作功能参考，绝不作为正式 UI 蓝本**。正式版应基于产品/设计稿重新规划首页信息层级、房间卡片、准备动效、聊天体验等。
- **视觉规范**：demo 的配色（深灰 hero + 绿色主色 `#2c9c68`）只是占位风格，正式版需按品牌/设计重新定义。
- **组件化**：demo 为单文件平铺，正式版应拆分可复用组件（房间卡片、成员头像、聊天气泡等）。

> ⚠️ **不要混淆**：重做的是「长什么样、怎么交互」；不重做的是「业务规则、数据结构、接口契约、权限」。哪怕 UI 全换，前端仍然必须按 §4 的字段和接口传参。

### 3.3 重做时需要「顺手补上」的遗留项（见 §6）
- `app.js` 的 `env` 占位符替换为真实云环境 ID。
- `project.config.json` 的 `appid` 由 `touristappid` 换成真实 AppID。
- 订阅消息模板 ID 配置（前端 `config/notification.js` + 云函数 `startRoom/notificationConfig.js`）。
- 过期房间定时清理任务（可选，二期）。

---

## 4. 技术上下文（重要决策记录）

### 4.1 数据结构设计 🔒

数据库：微信云开发 Cloud Database（文档型）。共 **4 个集合**。

#### `rooms`（房间）
| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 系统自动生成 |
| `game` | string | 预设枚举（无畏契约 / 三角洲行动 / CS2 / 英雄联盟 / 永劫无间）或自定义游戏名，trim 后 ≤20 字 |
| `inviteCode` | string | 4 位房间邀请码，createRoom 落地时随机生成（字符集去除易混淆的 I/O/0/1）；前端首页按码查询、详情页展示复制。历史存量房无此字段 |
| `mode` | string | 模式/地图，trim 后 ≤30 字 |
| `maxPlayers` | number(int) | 2–20 |
| `remark` | string | 备注，trim 后 ≤100 字 |
| `startTimeLabel` | string | 枚举：现在开打 / 今晚 / 明天 / 自定义时间 |
| `hostOpenid` | string | 房主 openid |
| `status` | string | `recruiting`/`full`/`pending`/`ready`/`dissolved` |
| `createdAt` | number | 创建时间戳(ms) |
| `updatedAt` | number | 更新时间戳(ms) |
| `expireAt` | number | 过期时间戳 = createdAt + 2h |
| `lastActiveAt` | number | 最后活跃时间（当前 createRoom 写入，后续未持续更新，属遗留） |
| `allReadyAt` | number\|null | 全员准备达成时间 |
| `dissolvedAt` | number | 解散时间 |
| `readyNotificationStatus` | string\|null | `pending`/`sent`/`partial`/`failed`/`not_configured`/`unavailable` |
| `readyNotificationAt` | number | 提醒发送时间 |
| `readyNotificationAttempted/Sent/Failed/Unsubscribed` | number | 提醒发送统计 |

#### `participants`（房间成员）
| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 系统生成 |
| `roomId` | string | 房间 ID |
| `openid` | string | 成员 openid |
| `displayName` | string | **当前实现中恒为 `''`**；展示用昵称在查询时实时 `join users` 得到（见 §4.3 getRoom） |
| `isHost` | bool | 是否房主 |
| `ready` | bool | 准备状态 |
| `createdAt` | number | 加入时间 |

#### `messages`（聊天消息）
| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 系统生成 |
| `roomId` | string | 房间 ID |
| `openid` | string | 发送者 openid |
| `content` | string | 内容，trim 后 ≤200 字 |
| `createdAt` | number | 发送时间 |

#### `users`（用户资料，隐私集合）
| 字段 | 类型 | 说明 |
|---|---|---|
| `_id` | string | 系统生成 |
| `openid` | string | 用户 openid |
| `gameNickname` | string | 游戏昵称，≤20 字 |
| `createdAt` / `updatedAt` | number | 时间戳 |

### 4.2 集合权限 🔒
- `rooms` / `participants` / `messages`：**所有用户可读，仅创建者可读写**（前端 `watch` 需要读权限）。
- `users`：**仅创建者可读写**（昵称隐私）。
- 云函数走服务端权限，不受此限制；前端展示与监听依赖上述读权限。

### 4.3 接口定义（云函数，12 个）🔒

> 所有云函数通过 `wx.cloud.callFunction({ name, data })` 调用，`data` 即下方 `event`。身份从 `cloud.getWXContext().OPENID` 获取，前端无需传 openid。

| 云函数 | 入参 `event` | 返回 | 关键行为 / 校验 |
|---|---|---|---|
| `createRoom` | `{game, mode, maxPlayers, remark, startTimeLabel}` | `{roomId}` | 校验 game 为预设枚举或 ≤20 字自定义名、maxPlayers∈[2,20]、startTimeLabel 枚举；mode≤30、remark≤100 截断；生成 4 位邀请码写入房间；创建房间(状态 recruiting)+房主成员；**成员写入失败补偿删除房间** |
| `joinRoom` | `{roomId}` | `{joined:true}` 或 `{joined:false,message:'已在房间内'}` | 拒绝：已解散 / 已 ready / 房主本人重复加入；满员则标记 full 并拒；**并发二次校验+补偿删除防超员** |
| `toggleReady` | `{roomId}` | `{ready, allReady, status}` | **事务**；非成员报错；成员≥2 且全 ready→`pending`，否则→`recruiting`；**不发通知** |
| `startRoom` | `{roomId}` | `{status:'ready', notification}` | 仅房主、且必须为 `pending`；→`ready` 锁房；发订阅消息；`notification.status`∈ sent/partial/failed/not_configured/unavailable |
| `leaveRoom` | `{roomId, targetOpenid?}` | `{left:true}` | 自己退出或房主移除(targetOpenid)；房主不可退出（须解散）；退出后若房间不再满/本为 ready，回落 recruiting |
| `dissolveRoom` | `{roomId}` | `{dissolved:true}` | 仅房主；软删除（status→dissolved） |
| `listRooms` | 无 | `{rooms:[...]}` | 仅 `status=recruiting` 且 `expireAt>now`，按 createdAt 降序，limit 50；含 memberCount |
| `listMyRooms` | 无 | `{rooms:[...]}` | 当前用户作为成员的全部未解散房间，含 roleText，房主优先再按 updatedAt 降序 |
| `getRoom` | `{roomId}` | `{room, participants, messages, isHost, myReady, isMember}` | room 含 inviteCode（供前端展示/复制邀请码）；实时 join `users` 取 displayName；messages≤100 按 createdAt 升序；返回 isMember 供前端决定自动入房 |
| `sendMessage` | `{roomId, content}` | `{messageId}` | 仅成员可发；content≤200；更新房间 updatedAt |
| `getProfile` | 无 | `{gameNickname}` | 读 users 取昵称（首次无记录返回 ''） |
| `saveProfile` | `{gameNickname}` | `{gameNickname}` | upsert users；昵称≤20，非空 |

**房间状态机** 🔒：
```
createRoom         → recruiting
joinRoom 满员      → full（达上限时）
toggleReady 全准备 → pending（≥2 人且都 ready）
startRoom(房主)    → ready（锁房，拒绝新成员）
leaveRoom 破坏满员 → recruiting（回落，保留其他成员 ready）
dissolveRoom       → dissolved（软删除）
```
> 注意：`pending` 阶段**不锁房**，新成员仍可加入（`joinRoom` 仅拒绝 dissolved/ready）；房主在 pending 点开始才进入 ready 锁房。

### 4.4 实时能力决策 🔒
- 采用**云数据库 `watch`**（客户端实时监听），不引入 WebSocket / 长轮询。
- `room-detail` 页面挂 3 个 watcher：`rooms.doc(roomId)`、`participants.where({roomId})`、`messages.where({roomId}).orderBy(createdAt,asc)`。房间状态变化直接 `formatRoom`；成员/消息变化触发 `getRoom` 全量刷新。
- `onUnload` 时统一 `close()` 三个 watcher，避免泄漏。

### 4.5 订阅消息决策 🔒
- 触发链：`pending` → 房主 `startRoom` → 遍历成员调 `cloud.openapi.subscribeMessage.send`。
- 配置两处（当前均为空占位）：
  - 前端 `miniprogram/config/notification.js` 的 `roomReadyTemplateId`（准备时请求订阅授权用）。
  - 云函数 `cloudfunctions/startRoom/notificationConfig.js` 的 `templateId` 与 `buildData()`（按实际模板字段实现）。
- **诚实行为**：未配置模板时 `startRoom` 仍正常成功，`notification.status='not_configured'`，前端明确提示「开打提醒尚未配置」，**绝不伪造已推送**。

### 4.6 工程化基础设施（建议正式版沿用）🔍
仓库已具备较完整的离线测试能力，**建议正式版保留这套测试思路**：
- **Jest 单测**：`tests/` 覆盖 10 个云函数 + 4 个页面，14 suite / 39 用例全过（运行 `npm test`）。
- **`tests/helpers/wxServerSdkMock.js`**：纯内存 mock `wx-server-sdk`，让云函数脱离云环境跑单测；提供 `__reset/__setOpenid/__seed/__collection` 控制 API。
- **`tests/helpers/miniprogramHarness.js`**：补齐 `Page/getApp/wx` 全局，`createPage()` 深拷贝页面实例做逻辑冒烟测试。
- **`demo/index.html` + `shots/*.py`**：用 HTML 镜像 + Playwright（Edge 浏览器，localhost:8000）做端到端验证与截图。**正式版重做前端后，应同步更新 demo 镜像或改用小程序真机/模拟器 E2E（如 miniprogram-simulate）。**
- **`coverage/`**：Jest 覆盖率报告已生成。

---

## 5. 正式版需求规格

### 5.1 功能范围
在 demo 已验证功能基础上，正式版应实现：

1. **发起上号**：游戏选择（预设 + 自定义输入）、人数（2–20，带游戏默认值）、模式/备注、开打时间标签；提交后跳转房间详情。
2. **招募大厅（首页）**：分「正在召集」与「我的房间」两区；只展示招募中且未过期房间；支持下拉刷新；提供邀请码输入直达房间（客户端直查 rooms，取最新未解散且未过期匹配）；点击进入房间。
3. **房间详情**：
   - 房间信息卡（游戏/模式/时间/人数/备注/状态徽标）。
   - 成员列表（头像首字、昵称、房主/队员标识、准备状态；房主可见移除按钮）。
   - 准备 / 取消准备（非房主与房主均可准备）。
   - 房主专属：「开始游戏」按钮（仅 pending 出现）、「解散房间」。
   - 非房主专属：「退出房间」。
   - 实时文字聊天（消息列表 + 输入框，≤200 字）。
   - 全员准备后展示「全员已准备」提示。
4. **分享与自动入房**：分享卡片带 `roomId`，非成员打开自动入房。
5. **资料页**：设置/保存游戏昵称（≤20 字）。
6. **订阅消息**：配置模板后，房主开始游戏向全体成员推送「开打提醒」；未配置时诚实提示。
7. **过期处理**：房间创建 2h 后从大厅消失（列表查询过滤）。

### 5.2 完成标准（Acceptance Criteria）🔒
- [ ] 12 个云函数行为、入参、返回、校验规则与 §4.3 完全一致（可对照现有单测）。
- [ ] 房间状态机严格遵循 §4.3 状态机，任何操作不出现脏状态（如 ready 房仍能加入、dissolved 房仍显示）。
- [ ] 并发加入不产生超员（可复现压测验证 `joinRoom` 补偿逻辑）。
- [ ] 前端传参与 §4.1/§4.3 字段完全一致，不因 UI 重做而改字段名/改语义。
- [ ] 集合权限按 §4.2 设置，`users` 昵称不可被他人读取。
- [ ] 实时监听在正常进入/退出页面时无泄漏（watcher 正确 close）。
- [ ] 分享链接进入能自动入房并展示房间。
- [ ] 过期房间不再出现在大厅，但数据保留、已在进行中的房间不受影响。
- [ ] 现有 Jest 单测（云函数 10 + 页面 4）在正式版代码上**全部通过**；前端重做后页面测试需同步更新。
- [ ] 订阅消息：配置模板后真实推送可达；未配置时页面明确提示且不影响开始流程。

### 5.3 部署前置条件（上线清单）
- [ ] `miniprogram/app.js` 的 `env` 替换为真实云环境 ID。
- [ ] `project.config.json` 的 `appid` 替换为真实小程序 AppID（当前 `touristappid`）。
- [ ] 云开发控制台创建 4 个集合（rooms/participants/messages/users）并设置 §4.2 权限。
- [ ] 建议建索引：rooms(`status+expireAt`)、participants(`roomId`)、messages(`roomId+createdAt`)。
- [ ] 部署 12 个云函数（右键「上传并部署：云端安装依赖」）。
- [ ] 小程序后台创建「全员准备提醒」订阅模板，回填 `notification.js` 与 `notificationConfig.js`。

---

## 6. 待办与风险

### 6.1 已知限制（来自当前实现）
- **订阅消息未真实可用**：模板 ID 为空，当前仅占位，开始游戏不会真发提醒。
- **过期无定时清理**：`expireAt` 仅用于列表过滤，无云函数定时触发器回收过期/已解散数据，长期运行会产生数据堆积。
- **`lastActiveAt` 未持续更新**：创建时写入后未随聊天/准备等活跃行为刷新，`listMyRooms` 排序用的 `updatedAt` 实际由各操作维护。
- **满员与 pending 的边界**：`joinRoom` 在 `pending` 阶段仍允许加入（设计如此，非 bug），但 UI 需让用户理解「待开始≠已锁房」。
- **聊天无分页/无未读**：`getRoom` 一次取 ≤100 条，高频聊天或长期房间需考虑分页与性能。
- **无加入/满人/过期提醒**：仅「开始游戏」有订阅消息，其余提醒属二期。

### 6.2 待确认事项（需与产品/你确认）
1. 正式版是否需要 **真实视觉设计稿**，还是由开发自行定义风格？
2. 是否需要扩展游戏列表、或支持用户自定义游戏？
3. 过期房间是否要做**定时物理清理**（云函数定时器）？
4. 是否需要「房间历史/战绩」「好友关系」「地理位置附近开黑」等扩展？
5. 订阅消息是否要覆盖「满员」「即将过期」等更多触发点？

### 6.3 改进方向
- 前端组件化、抽离公共样式与请求层（统一封装 `callFunction` 与错误提示）。
- 聊天接入分页或增量拉取，避免大房间性能问题。
- 将 `displayName` 的实时 join 改为在 `participants` 写入时冗余昵称（减少查询），或保留 join 但加缓存。
- 接入云函数定时触发器，定期清理 `expireAt` 过期且已无活跃成员的房间。
- 正式版 E2E 测试从「HTML 镜像 + Playwright」迁移到小程序真机/模拟器方案（如 `miniprogram-simulate` 或微信开发者工具 CI）。

---

## 附：快速索引
- 真实小程序前端：`miniprogram/`（**正式版重做 UI，保留传参/逻辑**）
- 浏览器镜像（仅测试用）：`demo/index.html` 🔍
- 云函数源码：`cloudfunctions/`（**全部保留**）🔒
- 单测与 E2E：`tests/`、`shots/`、`coverage/` 🔍
- 原 README（部署步骤）：`README.md`
- 任务管理（Trellis）：`.trellis/`、`AGENTS.md`
